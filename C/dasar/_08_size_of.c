/*size of akan  menghitung lebar dari sebauh array*/

#include <stdio.h>
int main(){
	char food[] = "saya";
	int len = sizeof(food);
	printf("Enter favorite food: %s \n", food);
	printf("Enter favorite food: %i \n", len);
	return 0;
}
