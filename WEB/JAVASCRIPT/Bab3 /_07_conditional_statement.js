/* looking for an exact value match for the string "tim" */
if(person === "tim") {
	alert(person + ", this is me");
} 
else {
	alert(person);
}


switch (person) {
	case "tim":
		alert("this is me");
	break;
	case "christine":
		alert("my sister");
	break;
	default:
		alert(person);
}

